# LLM · LangGraph · RAG 확장안

## 추천 기능: AI 생산계획 도우미

예측 모델이 수량을 계산하고 LLM은 계산 결과를 설명합니다. RAG는 매장 문서에서
근거를 찾고 LangGraph는 검증부터 점주 승인까지 실행 순서를 관리합니다. LLM이
생산량을 직접 만들거나 모델 결과를 임의로 바꾸지 않습니다.

```text
판매기록 검증
  → 최종 결합 예측 모델 실행
  → 레시피·생산지침·과거 메모 검색(RAG)
  → 모델 결과와 검색 근거를 사용한 설명 생성(LLM)
  → 점주가 수량 확인·수정
  → 생산계획 저장
```

## MVP에 넣기 좋은 화면 기능

1. **추천 이유 보기**
   - P50, P75, 최근 판매 추세, 기록 수, 품절 여부를 쉬운 한국어로 설명
   - 사용한 숫자는 FastAPI 모델 응답에서만 가져옴
2. **점주 질문창**
   - “왜 어제보다 20개 늘었어?”, “비가 오면 무엇을 확인해야 해?”에 답변
   - 수량 질문에는 모델 결과를 인용하고, 운영 질문에는 RAG 문서를 인용
3. **생산 체크리스트**
   - 추천 수량과 레시피의 배치 크기·오븐 용량·굽는 시간을 결합
   - 최종 결정 전 점주 승인 단계 유지
4. **폐기 회고 요약**
   - 실제 판매·폐기·품절 기록을 주간 단위로 요약
   - 모델 재학습과 분리하며 수치 집계는 일반 코드가 담당

## RAG에 넣을 실제 자료

- 상품별 레시피와 1회 생산 단위
- 오븐 용량과 시간대별 생산 가능량
- 보관·폐기 기준
- 예약 주문 처리 규칙
- 행사·휴일 운영 메모
- 과거 생산 실패 및 품절 메모

문서가 없다면 RAG를 먼저 넣지 않습니다. 최소한 위 자료를 Markdown 또는 PDF로
준비하고, 답변에 문서명과 구절을 함께 표시해야 시연에서도 역할이 분명합니다.

## LangGraph 상태 예시

```text
sales_history, inventory, reservations, target_date
prediction, retrieved_documents, explanation
approved_quantity, approved_by, saved_at
```

노드는 `validate_input → run_forecast → retrieve_context → explain → human_approval
→ save_plan` 순서가 적당합니다. 모델 또는 검색이 실패하면 설명 생성을 건너뛰고
예측 결과와 오류를 그대로 보여주는 분기를 둡니다.

## 구현 순서

1. React ↔ FastAPI 예측 연결을 완성하고 고정된 API 계약을 검증
2. 매장 문서 5~10개를 준비하고 출처가 보이는 RAG 질문 기능 추가
3. LLM 설명에 모델 응답과 검색 문서만 제공
4. 흐름이 세 단계 이상 안정된 뒤 LangGraph로 승인·저장 상태를 관리

API 키와 LLM 제공자는 이후 선택합니다. 예측 API는 LLM 없이도 항상 동작해야 합니다.
